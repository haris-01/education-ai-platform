import { generatePaper } from './generatePaper'
import { getAllPapers } from './getAllPapers'
import { getPaper } from './getPaper'
import { getPaperPdf } from './getPaperPdf'

export const paperControllers = {
  generatePaper,
  getAllPapers,
  getPaper,
  getPaperPdf,
}
